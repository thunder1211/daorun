{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy:gh": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "deploy:gh2": "gh-pages -d dist"
  },
  "devDependencies": {
    "@types/node": "^18",
    "gh-pages": "^5.0.0",
    "nuxt": "^3.4.3",
    "push-dir": "^0.4.1",
    "sass": "^1.62.1"
  },
  "dependencies": {
    "ant-design-vue": "^3.2.20"
  }
}
